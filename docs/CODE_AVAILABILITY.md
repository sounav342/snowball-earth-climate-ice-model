# Suggested Code-Availability Statement

The MATLAB source code for the one-dimensional coupled atmosphere, saline slab-ocean, and dynamic sea-glacier model is available at `[repository URL]`. The archived version used for the publication is available at `[archive DOI]`. The repository includes initial-condition files, tests, and instructions for reproducing individual experiments. Simulations were performed with MATLAB `[release]`; the exact code version is commit `[commit hash]`.

Replace all bracketed fields before submission.
